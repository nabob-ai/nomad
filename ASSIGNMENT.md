# 全栈开发测试：Nomad Concierge

## 背景

Nomad Concierge 是一个面向计划到海外生活的用户的服务入口。目标用户主要是具备远程工作或跨境生活可行性的个人和家庭。用户可能同时需要处理软移民、公司注册、税务年审、医疗和子女教育等问题，但通常不知道需要先提供哪些信息，也不知道下一步该做什么。

长期设想是覆盖 200 多个国家和地区，并在必要时对接持牌服务机构；本次测试不要求实现全球知识库，也不要求覆盖物流、商业租赁、化工等强地域依赖行业。

本次测试请你完成一个最小可用 Web 应用，使用户能够从一段自然语言描述开始，经过 Agent 的主动追问，最终获得一份结构化、可继续执行的初步行动计划。

示例用户的初始描述可能是：

> 我和配偶打算明年带孩子搬到葡萄牙生活，想开一家公司，也需要了解报税和孩子上学的问题，请告诉我该怎么办。

## 第一阶段：先发现需求

这份说明有意没有穷举所有产品规则。开始编码前，请提交最多 12 个有优先级的澄清问题。

要求：

- 问题应帮助你理解用户、业务目标、范围、风险和验收方式。
- 可以将密切相关的问题组合为一个问题，但不要提交长篇调查问卷。
- 收到面试官答复后，再开始 6 小时开发计时。
- 将最终采用的假设、范围和取舍记录在 `DECISIONS.md`。

如果面试官没有回答某项细节，你仍可作出合理假设，但必须记录理由。

## 需要交付的最小闭环

应用应至少能够：

- 接收用户的初始需求描述；
- 通过多轮交互主动补充完成服务计划所需的信息；
- 由后端保存会话和业务数据，页面刷新后仍能继续；
- 在信息达到你定义的条件后生成结构化初步行动计划；
- 清楚处理加载、无数据、输入错误和服务失败等状态；
- 让一位不懂技术的面试官按照 README 在本地运行和验证。

你需要自行确定数据模型、API、交互方式、Agent 工作机制以及计划的内容结构。我们更看重完整而可靠的窄范围闭环，而不是大量未完成的功能。

## 技术约束

- 编程语言、前端框架、后端框架和数据库类型均由你选择，技术选择本身不会影响评分。
- 随题提供的 React + Node.js 项目仅为可选参考模板；你可以使用、修改或完全不用它。
- 必须提供可在浏览器中使用的 Web 界面、由你实现的服务端业务逻辑，以及真实的持久化存储。
- 可以采用前后端分离架构，也可以使用具备服务器端逻辑的全栈或服务端渲染框架。
- 不能仅使用静态页面、浏览器本地存储或纯前端模拟代替后端和数据库。
- 可使用 SQLite、PostgreSQL、MySQL 或其他合理的持久化方案。为方便本地验收，推荐选择无需复杂外部配置的方案。
- 可以增加依赖或第三方服务，但需说明用途；核心业务判断和数据流必须能够在提交代码中接受检查。
- README 必须注明所需运行环境、版本、安装步骤、启动命令和技术选型理由。提供一条命令启动或 Docker 配置可加分，但不是硬性要求。
- 应用必须在没有付费 API 密钥的情况下可运行和验收。
- 可以选择接入真实大模型，但必须同时提供可重复运行的本地替代方案。
- 不需要提供真实的法律、税务、移民或教育专业意见。
- 不得提交真实个人信息、真实客户数据或有效密钥。

## AI 工具使用

允许并鼓励使用 Cursor、Codex、Claude Code、GitHub Copilot 或其他 AI 工具。

请填写 `AI_USAGE.md`，说明：

- 使用了哪些工具及用途；
- 2～3 个关键交互或提示词摘要；
- 发现并修正过哪些 AI 输出问题；
- 如何验证生成代码的正确性、安全性和可维护性。

无需提交私人账号中的完整对话记录，但你必须能够在复盘中解释和修改所提交的代码。

## 时间限制

- 收到澄清答复后，48 小时内提交。
- 实际投入最多 6 小时。
- 未完成全部设想不构成直接失败；请优先交付可运行闭环，并如实记录未完成项。

## 提交内容

- 完整源代码；
- `README.md`：安装、启动、测试和验收步骤；
- `DISCOVERY.md`：你提出的问题和收到的答复；
- `DECISIONS.md`：范围、假设、重要决策、取舍与未完成项；
- `AI_USAGE.md`：AI 使用说明；
- 必要的数据库迁移或初始化方式；
- 至少一项有意义的自动化测试。

提交前请查看 `SUBMISSION_CHECKLIST.md`。

## 复盘说明

提交后将进行约 45 分钟的线上复盘。你需要演示主要流程、解释关键实现，并讨论一个临时需求变化。复盘关注你对作品的理解与判断，不要求背诵代码。
